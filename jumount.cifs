#!/bin/bash

MOUNTS=`cat ~/.jarvis-mounts`

echo -n "Enter password: "
read -s pw
echo ""

echo $pw | sudo -S umount $HOME/jarvis
echo $pw | sudo -S umount $HOME/shared

for mount in $MOUNTS; do
    echo $pw | sudo -S umount $HOME/$mount
done

